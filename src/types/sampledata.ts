export type sampleData = {
    [key: string]: boolean[]
}
export type Sample = {
    id?: number,
    api_key?: string,
    name?: string,
    recording_data: string,
    type: string;
    datetime?: string;
}

